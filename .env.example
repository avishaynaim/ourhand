# ============================================
# Yad2 Monitor - Environment Variables
# ============================================

# Required - Telegram Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Telegram Webhook (optional - for webhook-based multi-user bot)
# Set this to your public URL for production deployment
# Example: https://yourdomain.com
# Leave empty for polling mode (development)
TELEGRAM_WEBHOOK_URL=

# Yad2 Search URL (optional - can add multiple via API)
YAD2_URL=https://www.yad2.co.il/realestate/rent?topArea=41&area=12&city=8400

# Scraping Intervals (minutes)
MIN_INTERVAL_MINUTES=60
MAX_INTERVAL_MINUTES=90

# Web Dashboard
ENABLE_WEB=true
WEB_PORT=5000

# API Security (recommended for production)
# Generate a secure random string for API authentication
# Example: openssl rand -hex 32
API_KEY=your_secret_api_key_here

# CORS Configuration (comma-separated list of allowed origins)
# Use '*' for all origins (not recommended for production)
# Example: http://localhost:5000,https://yourdomain.com
ALLOWED_ORIGINS=*

# Flask Environment (set to 'production' for production deployments)
# Affects security checks for API_KEY requirement
FLASK_ENV=development

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Proxy Configuration (optional)
# Format: ip:port,ip:port:user:pass
PROXY_LIST=
# Or single proxy:
HTTP_PROXY=
HTTPS_PROXY=
