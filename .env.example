# ============================================
# OurHand - Full Israel Rentals Monitor
# ============================================
# IMPORTANT: Use a DIFFERENT Telegram bot for this project!
# Create a new bot via @BotFather for ourhand deployment.
# Do NOT reuse the same bot token as Myhand project.

# Required - Telegram Configuration (CREATE A NEW BOT!)
# 1. Message @BotFather on Telegram
# 2. Send /newbot and follow instructions
# 3. Copy the token here
TELEGRAM_BOT_TOKEN=your_NEW_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Telegram Webhook (MUST be different URL than Myhand!)
# Set this to your ourhand Railway public URL
# Example: https://ourhand-production.up.railway.app
TELEGRAM_WEBHOOK_URL=

# Yad2 Search URL - ALL Israel rentals (700+ pages, 29K+ apartments)
YAD2_URL=https://www.yad2.co.il/realestate/rent

# Scraping Intervals (minutes) - More frequent with smart-stop
MIN_INTERVAL_MINUTES=20
MAX_INTERVAL_MINUTES=40

# Web Dashboard
ENABLE_WEB=true
WEB_PORT=5000

# API Security (recommended for production)
# Generate a secure random string for API authentication
# Example: openssl rand -hex 32
API_KEY=your_secret_api_key_here

# CORS Configuration (comma-separated list of allowed origins)
# Use '*' for all origins (not recommended for production)
# Example: http://localhost:5000,https://yourdomain.com
ALLOWED_ORIGINS=*

# Flask Environment (set to 'production' for production deployments)
# Affects security checks for API_KEY requirement
FLASK_ENV=development

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Proxy Configuration (optional)
# Format: ip:port,ip:port:user:pass
PROXY_LIST=
# Or single proxy:
HTTP_PROXY=
HTTPS_PROXY=
